datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model ToggleState {
  id     Int     @id
  active Boolean @default(false)
  name   String?
}